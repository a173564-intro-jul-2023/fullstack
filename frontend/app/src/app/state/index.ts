import { ActionReducerMap } from '@ngrx/store';

export interface AppState {} // what the application "state" looks like for typescript

export const reducers: ActionReducerMap<AppState> = {}; // handles all activity of that state
